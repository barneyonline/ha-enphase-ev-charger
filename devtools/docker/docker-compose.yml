services:
  ha-dev:
    build:
      context: ../..
      dockerfile: devtools/docker/Dockerfile
    volumes:
      - ../..:/workspace:delegated
    working_dir: /workspace
    command: sleep infinity
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTEST_DISABLE_PLUGIN_AUTOLOAD=1

  ha-runtime:
    image: ghcr.io/home-assistant/home-assistant:stable
    restart: unless-stopped
    ports:
      - "8123:8123"
    environment:
      - TZ=${TZ:-UTC}
    dns:
      - ${HA_RUNTIME_DNS_1:-1.1.1.1}
      - ${HA_RUNTIME_DNS_2:-8.8.8.8}
    volumes:
      - ../../.ha-config:/config
      - ../../custom_components/enphase_ev:/config/custom_components/enphase_ev:ro
